## Running Locally
1. Clone this repo
2. Run `make setup` to install dependencies, `make pipeline` to build the database, and `make dashboard` to launch the dashboard
3. Open the local URL shown in your terminal to access the dashboard

## Project Structure
- `requirements.txt` - installs necessary dependencies
- `load_data.py` - Executes Parts 1 - 4
- `dashboard.py` - Streamlit app presenting tables and plots
- `Makefile` - automates the full pipeline

## Live Dashboard Link
https://automatic-space-garbanzo-46xv6w7xrxg3jwpq-8501.app.github.dev/
